{
  "name": "@job-orchestrator/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --configLoader runner",
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs"
  },
  "dependencies": {
    "@job-orchestrator/contracts": "0.1.0"
  }
}
